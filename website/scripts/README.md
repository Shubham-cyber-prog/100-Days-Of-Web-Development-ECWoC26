## Authentication Setup

This project supports Firebase Authentication.

Steps:
1. Create a Firebase project
2. Enable Email/Password authentication
3. Copy `website/scripts/firebaseConfig.example.js`
4. Rename it to `firebaseConfig.js`
5. Paste your Firebase credentials
